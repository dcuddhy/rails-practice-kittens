NEW


<%= form_for [@kitten, @categorization] do |f| %>

<%= f.label :category %>

<%= f.collection_select(
  :category_id,
  Category.all,
  :id,
  :name,
  {include_blank: "Please choose a category..."},
  {}
) %>

<%= f.submit "How Cute is THAT?" %>



<% end %>
